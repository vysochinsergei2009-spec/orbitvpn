1. Install Marzban-Node:
'''
sudo bash -c "$(curl -sL https://github.com/Gozargah/Marzban-scripts/raw/master/marzban-node.sh)" @ install
'''
2. Enter Token

3. Open Ports:
'''
sudo ufw allow 62050/tcp
sudo ufw allow 62051/tcp
sudo ufw status
'''
(if default)
