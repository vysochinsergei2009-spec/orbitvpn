# 🚀 Быстрый старт OrbitVPN Manager

## За 3 минуты до запуска

### 1️⃣ Откройте Xcode (если еще не установлен)

```bash
# Установите Xcode из App Store (бесплатно)
# Или скачайте с developer.apple.com
```

### 2️⃣ Создайте Xcode проект

**В Xcode:**

1. **File** → **New** → **Project**
2. Выберите **macOS** → **App**
3. Нажмите **Next**
4. Заполните:
   - **Product Name**: `OrbitVPNManager`
   - **Team**: Ваш Apple ID (можно оставить None для разработки)
   - **Organization Identifier**: `com.orbitvpn`
   - **Interface**: **SwiftUI**
   - **Language**: **Swift**
   - **Use Core Data**: Снять галочку
   - **Include Tests**: Снять галочку
5. **Next** → Выберите папку `mac/` → **Create**

### 3️⃣ Замените файлы

Xcode создал пустой проект. Теперь замените файлы нашими:

```bash
# Удалите автосгенерированные файлы:
cd mac/OrbitVPNManager
rm OrbitVPNManagerApp.swift ContentView.swift

# Наши файлы уже на месте! Просто перетащите их в Xcode:
# 1. Откройте Finder
# 2. Перейдите в mac/OrbitVPNManager
# 3. Перетащите все .swift файлы в Xcode в Project Navigator
```

**Структура должна быть:**

```
OrbitVPNManager (проект в Xcode)
├── OrbitVPNManagerApp.swift
├── Models/
│   └── Models.swift
├── Services/
│   ├── APIService.swift
│   └── BackendManager.swift
└── Views/
    ├── ContentView.swift
    ├── DashboardView.swift
    ├── ServicesView.swift
    ├── UsersView.swift
    ├── MarzbanView.swift
    ├── BroadcastView.swift
    ├── LogsView.swift
    └── SettingsView.swift
```

### 4️⃣ Добавьте Backend в Resources

1. В Xcode Project Navigator → Правый клик на проект
2. **Add Files to "OrbitVPNManager"...**
3. Выберите папку `mac/backend/`
4. ✅ Включите **"Copy items if needed"**
5. ✅ Включите **"Create folder references"**
6. **Add**

### 5️⃣ Настройте Build Settings

В Xcode:
1. Выберите проект в Navigator
2. Вкладка **Build Settings**
3. Найдите **Deployment Target**
4. Установите: **macOS 13.0**

### 6️⃣ Запустите! 🎉

1. Выберите схему **OrbitVPNManager** (My Mac)
2. Нажмите **⌘ + R** или кнопку ▶️
3. Приложение запустится и автоматически стартует Python backend!

---

## Альтернативный способ (без Xcode GUI)

Если хотите создать проект через командную строку:

```bash
cd mac/OrbitVPNManager

# Создать минимальный project.pbxproj
# (Этот файл слишком большой для ручного создания)
# Рекомендуется использовать Xcode GUI выше
```

---

## Что делает приложение при запуске?

1. ✅ Запускает Python backend на порту 8080
2. ✅ Подключается к API
3. ✅ Загружает статистику dashboard
4. ✅ Готово к использованию!

---

## Горячие клавиши

- **⌘ + R** - Запустить приложение
- **⌘ + .** - Остановить приложение
- **⌘ + B** - Собрать проект
- **⌘ + Shift + K** - Очистить build

---

## Следующие шаги

✅ Приложение запущено!

Теперь вы можете:
- 📊 Смотреть статистику в Dashboard
- 🖥️ Управлять сервисами
- 👥 Мониторить пользователей
- 🌐 Проверять Marzban серверы
- 📢 Делать рассылки
- 📋 Читать логи в реальном времени

---

## Troubleshooting

### "Command Line Tools" не найдены

```bash
xcode-select --install
```

### Python backend не запускается

```bash
# Проверьте Python:
python3 --version  # Должно быть 3.10+

# Установите зависимости:
pip3 install -r requirements.txt

# Запустите вручную для проверки:
cd mac/backend
python3 -m uvicorn manager.web.app:create_app --factory --host 127.0.0.1 --port 8080
```

### "No such module" ошибки в Xcode

1. Clean Build Folder: **⌘ + Shift + K**
2. Перезапустите Xcode
3. Пересоберите: **⌘ + B**

---

**Готово!** 🎊 Теперь у вас полноценный macOS менеджер для OrbitVPN!
